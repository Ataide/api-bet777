export enum BetType {
  Unica = "Única",
  Multipla = "Múltipla",
}

export enum TransactionType {
  deposit = "Depósito",
  withdraw = "Saque",
}

export enum BetResult {
  Vitoria = 1,
  Aberto = 0,
  Derrota = -1,
}

export enum BetChoice {
  Casa = 1,
  Empate = 0,
  Visitante = -1,
}
